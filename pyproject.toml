[project]
name = "claude-json2md4notebooklm"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[tool.uv]
package = false

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = ["-p", "no:cacheprovider"]

[dependency-groups]
dev = [
    "pytest>=9.0.3",
]
